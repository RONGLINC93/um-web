#!/bin/bash
# 安装后：纯前端静态站点，无持久化数据需要处理，仅记录日志
echo "[$(date '+%Y-%m-%d %H:%M:%S')] install_callback ${TRIM_APPNAME:-unknown} ${TRIM_APPVER:-unknown}" >> "${TRIM_PKGVAR:-/tmp}/install.log" 2>/dev/null
exit 0
