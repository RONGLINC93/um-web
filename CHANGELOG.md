# 更新日志（CHANGELOG）

## [1.10.8] - 2026-09-23

### 新增

- **导入即入列表**：文件被导入后立即出现在任务列表中，无需等待解密完成，列表中直接显示该文件的**加密格式**（`rawExt`，如 NCM / QMCFLAC / KGM）。
- **任务状态列**：新增「状态」列，支持四态 —— 排队解锁中 / 正在解锁中（呼吸动画）/ 解锁完成 / 解锁失败。
- **串行解锁**：解密改为严格一个一个执行，上一个文件彻底结束才开始下一个，状态逐行推进。
- **按文件转 MP3**：转换是单个文件的行为，而不是下载时的隐式行为。
  - 每行新增「转 MP3」按钮，工具栏新增「选中转 MP3」，转换**就地生效**：该行的 `blob` / `ext` / `mime` / `file` 直接替换为 MP3，列表「格式」列变为 MP3。
  - 下载不再转码，按各行**实际格式**导出，因此一次可下载「部分 MP3 + 部分原格式」的混合集合。
  - 转换使用独立串行队列；**可取消**：行内在转换时显示取消按钮，工具栏在有待转任务时显示「取消转换」。
- **底部状态栏计数**：新增「已解锁」「解锁中」「已转 MP3」。

### 修复

- **进度条卡住**：`DecryptQueue.consume()` 原为 `.then(() => this.consume)`（漏写调用括号），任务完成后队列不会自动续跑；且每次入队都立即启动任务，导致批量文件全部并发、主线程被占满。现改为带 `running` 标志的严格串行队列。
- **Worker 初始化超时导致永久卡死**：线上报 `Timeout: Did not receive an init message from worker after 10000ms`，此时 threads.js 的任务 Promise 永不结算，进度条会卡在 `8 / 16`。已移除 Worker 线程池，统一走主线程串行队列，构建产物不再包含 worker chunk。
- **统计脚本未定义导致崩溃**：`window._paq.push(...)` 在 Matomo 未加载或被拦截时抛 `TypeError: Cannot read properties of undefined (reading 'push')`，改为 `trackEvent()` 内部做存在性判断。
- **不可达分支**：`src/decrypt/index.ts` 中重复的 `case 'mflach'`（前一个分支已命中）删除；字符串 `throw` 改为 `new Error(...)`，避免异常经 Worker 序列化丢失信息。
- **文件名解析边界**：`SplitFilename()` 处理无扩展名文件时返回空文件名，现返回完整文件名 + 空扩展名。
- **转 MP3 时页面卡死**：lamejs 编码是主线程上的同步长任务，整首歌编码期间页面完全无法操作。编码循环改为**分片执行**，每约 40ms `await` 让出主线程一次，转换过程中可正常滚动、勾选、播放。
- **转 MP3 不可取消**：新增 `ConvertCancelled` 与 `shouldCancel` 回调，在分片间隙检查取消标志，可中止单个或全部转换任务。

### 变更

- 未解锁完成的行：播放 / 下载 / 编辑按钮禁用，避免点到空数据。
- 「全部下载」「下载选中」只作用于已解锁完成的行。
- 单文件解密增加 120 秒超时兜底，超时标记失败并提示，不再无限等待。
- 拖拽区文案由「工作模式: 多线程 Worker / 单线程 Queue」改为「串行解锁：导入后按文件一个一个依次解锁」。
- **进度条改为行内**：移除导入区的总进度条（`8 / 16`）与转码时的居中遮罩弹窗，进度只在列表对应行显示；排队与终态不显示进度条，仅「正在解锁中」「转换 MP3 中」显示。
- **状态扩展**：在四态基础上新增 `转换 MP3 中`（真实百分比）、`等待转换 MP3`（排队）、`MP3 转换完成`，转换状态与解锁状态分开。
- **移除「输出音频格式」导航**（MP3 / 原始格式）与相关自动转码：改由每个文件手动或批量转换，避免"下载时才偷偷转码"的隐式行为。
- **列表格式徽章**：「加密格式」列按平台着色（灰芯片 + 平台色圆点），「格式」列按音频编码使用轻量淡色芯片（MP3 绿 / FLAC 蓝 / WAV 橙 / M4A 紫 / OGG 青 / APE 棕）。
- **减少弹窗通知**：移除「解锁成功」「保存成功」「修改成功 / 修改取消」等逐文件弹窗（状态列已体现），仅保留失败类提示与打包结果提示。

## 早期改动（2026-09-21 ~ 2026-09-22）

为本次记录前的既有改动，细节见 `README.md`「二次开发修改说明」：

- **应用封装**：新增 fnOS 应用 `.fpk` 离线封装（Windows 一键打包脚本），内置 Node 静态服务器。
- **界面重构**：改为左侧格式导航树 + 顶部工具栏 + 任务列表 + 底部状态栏的布局，列表紧凑化、右下角悬浮播放器、设置 / 关于弹窗。
- **交互**：任务区支持拖拽添加文件，与左侧拖拽区共用同一解密队列。
- **修复**：`src/main.ts` 补充 `RadioGroup` / `RadioButton` 按需注册（此前「输出格式」控件渲染为不可点击的死文本）。
- **修复**：`cmd/main` 改用 `setsid` 启动，`server.js` 增加 `listening` / `error` 日志，解决 fnOS 生命周期脚本退出时服务被误杀导致白屏。
- **调整**：移除启动时的更新检查弹窗及其联网请求；「请勿直接运行源代码！」提示改为仅在 `file://` 直接打开时显示。
